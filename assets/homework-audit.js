/* Homework Audit */
(function(){
'use strict';
let sb=null,ME=null,ACTIVE='logs',DATA={logs:[],errors:[]};
const cfg=()=>window.AMIN_CONFIG||{};const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
function client(){if(sb)return sb;sb=supabase.createClient(cfg().supabaseUrl,cfg().supabaseAnonKey,{auth:{persistSession:true,autoRefreshToken:true,storageKey:(cfg().authStorageKey||'amin-ovcjzsrqqgjsbqswtkro-auth-v2')}});return sb}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
function toast(t,m,type=''){const el=$('#toast');if(!el)return;el.innerHTML=`<b>${esc(t)}</b><br><span class="muted">${esc(m||'')}</span>`;el.className='toast show '+type;clearTimeout(el._t);el._t=setTimeout(()=>el.classList.remove('show'),4200)}
function fmt(v){if(!v)return'—';try{return new Date(v).toLocaleString('ar-IQ')}catch{return v}}
function roleLabel(r){return ({admin:'إدارة',teacher:'معلم',academic:'أكاديمي',academic_admin:'إدارة أكاديمية',scientific:'علمي',supervisor:'مشرف'}[r]||r||'مستخدم')}
async function ensure(){const {data:{session}}=await client().auth.getSession();if(!session){location.href='index.html';return false}const {data:u}=await client().from('users').select('*').eq('id',session.user.id).maybeSingle();if(!u){location.href='index.html';return false}ME=u;const ok=u.is_super_admin||['admin','teacher','academic','academic_admin','scientific','supervisor'].includes(u.role);if(!ok){document.body.innerHTML='<main class="login-page"><section class="login-card"><h1>غير مصرح</h1></section></main>';return false}$('#profileName').textContent=u.name||u.email;$('#profileRole').textContent=roleLabel(u.role);return true}
async function load(){try{const {data,error}=await client().rpc('get_homework_audit_payload',{p_limit:200});if(error)throw error;if(data&&data.ok===false)throw new Error(data.message||'تعذر التحميل');DATA={logs:data.logs||[],errors:data.errors||[]}}catch(e){toast('تعذر تحميل السجل',e.message||String(e),'red');DATA={logs:[],errors:[]}}render(ACTIVE)}
function render(id){ACTIVE=id;$$('.view').forEach(v=>v.classList.toggle('active',v.id==='view-'+id));$$('.nav button[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===id));id==='errors'?errorsView():logsView()}
function logsView(){const rows=DATA.logs.map(l=>`<tr><td>${esc(l.action)}</td><td>${esc(l.entity_table||'—')}</td><td>${esc(l.actor_name||l.actor_id||'—')}</td><td>${fmt(l.created_at)}</td><td><details><summary>بيانات</summary><pre>${esc(JSON.stringify({old:l.old_data,new:l.new_data,metadata:l.metadata},null,2))}</pre></details></td></tr>`);$('#view-logs').innerHTML=`<div class="page-head"><div><h1>سجل العمليات</h1><p>آخر عمليات إنشاء/تعديل/تصحيح الواجبات.</p></div></div>${table(['العملية','الجدول','المستخدم','الوقت','التفاصيل'],rows,'لا توجد سجلات')}`}
function errorsView(){const rows=DATA.errors.map(e=>`<tr><td>${esc(e.module||'—')}</td><td>${esc(e.message||'—')}</td><td>${fmt(e.created_at)}</td><td><details><summary>تفاصيل</summary><pre>${esc(JSON.stringify(e.details||{},null,2))}</pre></details></td></tr>`);$('#view-errors').innerHTML=`<div class="page-head"><div><h1>أخطاء الواجبات</h1><p>آخر الأخطاء المسجلة لمتابعة المشاكل بسرعة.</p></div></div>${table(['الوحدة','الرسالة','الوقت','التفاصيل'],rows,'لا توجد أخطاء')}`}
function table(h,rows,empty='لا توجد بيانات'){const body=Array.isArray(rows)?rows.join(''):String(rows||'');return body.trim()?`<div class="table-wrap"><table><thead><tr>${h.map(x=>`<th>${esc(x)}</th>`).join('')}</tr></thead><tbody>${body}</tbody></table></div>`:`<div class="empty">${esc(empty)}</div>`}
function bind(){$$('.nav button[data-view]').forEach(b=>b.addEventListener('click',()=>render(b.dataset.view)));$('#mobileMenuBtn')?.addEventListener('click',()=>$('#sidebar').classList.toggle('open'));$('#logoutBtn')?.addEventListener('click',async()=>{await client().auth.signOut({scope:'local'});location.href='index.html'});$('#refreshBtn')?.addEventListener('click',load)}
async function init(){client();if(!await ensure())return;bind();await load()}
window.HomeworkAudit={init,render};
}());
